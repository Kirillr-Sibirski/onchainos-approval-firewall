# X Post Draft

`OKX Approval Firewall` is live.

An agent-native approval firewall for `X Layer` wallets that:

- inspects live ERC-20 approval exposure
- scores spender risk under local policy
- replaces unlimited approvals with exact allowances
- executes cleanup through `Agentic Wallet`
- writes audit-ready proof artifacts

Built for `OKX Build X` in `Skills Arena`.

Core thesis:

`agents need a permission firewall, not just a revoke button`

Live X Layer proof:

- setup unlimited approval
- revoke unlimited approval
- exact-budget remediation onchain

Repo:
https://github.com/Kirillr-Sibirski/okx-buildx

#onchainos @XLayerOfficial
